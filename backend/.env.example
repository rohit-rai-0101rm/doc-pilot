# Supabase: Dashboard → Project Settings → API
# Database: Dashboard → Project Settings → Database → Connection string

# --- Supabase (Auth + API) ---

SUPABASE_URL=https://your-project-ref.supabase.co
SUPABASE_ANON_KEY=your-anon-public-key
SUPABASE_SERVICE_ROLE_KEY=your-service-role-secret-key

# --- Postgres (Alembic + direct DB access) ---

# Use the DIRECT connection (host: db.<ref>.supabase.co).
# Do NOT use the transaction pooler URL (pooler.supabase.com) — migrations require a session connection.
# URL-encode special characters in the password.
DATABASE_URL=postgresql://postgres:your-password@db.your-project-ref.supabase.co:5432/postgres

# --- xAI / Grok (LLM + embeddings via OpenAI-compatible API) ---
# Key: https://console.x.ai — Models: https://docs.x.ai/docs/models

XAI_API_KEY=xai-your-api-key-here
XAI_BASE_URL=https://api.x.ai/v1
XAI_CHAT_MODEL=grok-3-mini
XAI_EMBEDDING_MODEL=embed-large-v1
# Must match the embedding model output — verify before running migrations, then set pgvector column to match.
XAI_EMBEDDING_DIMENSIONS=1024

# --- Server ---

# Comma-separated browser origins allowed to call the API (CORS).
ALLOWED_ORIGINS=http://localhost:5173
